// array, object
// tuple

// explicit define
let bazarList: string[] = ["Milk", "eggs", "onion"];
// bazarList.push(true);  // can't push anything other than string

// implicit define: (string | number)[]
let mixedArray = ["Milk", 2, "eggs", 12,  "onion", 6];

let destination: [string, string, number] = ["Dhaka", "Canada", 2000];